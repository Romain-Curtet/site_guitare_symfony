<table class='table'>
    <tr>
        <td><a href='{{ path('product', { 'slug' : product.slug}) }}'><img src='../uploads/{{ product.illustration }}' alt='{{ product.name }}' height='200px'></a></td>
        <td class='name'>{{ product.name }}</td>
    </tr>
    <tr>
        <td>{{ product.category}}</td>
        <td>{{ (product.price / 100)|number_format(2, ',', ' ')}} €</td>
    </tr>
</table>